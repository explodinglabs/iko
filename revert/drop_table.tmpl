BEGIN;

!! Recreate the table here !!
For help, you can use:
- The create_table deploy script (plus later alterations)
- docker exec -it postgres pg_dump -U postgres --schema-only -t [% schema ].[% table %] [dbname]

COMMIT;
