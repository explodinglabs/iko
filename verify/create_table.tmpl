do $$
BEGIN

assert (
    select exists (
        select 1 from information_schema.tables
        where table_schema = '[% schema_name %]'
        and table_name='[% table_name %]'
    )
);

END; $$;
